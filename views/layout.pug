doctype html
html
  head
    title= title
    meta(name="description" content="This is a SalesForce Einstein Intent enabled seach, which extracts the user's intent on the seach and redirect them to the proper page or website.")
    meta(name="keywords" content="REI, Einstein, Einstein Intent, SalesForce, Sales Force")
    meta(name="author" content="Dipenra Rai")
    meta(name="viewport" content="width=device-width,initial-scale=1,minimum-scale=1,user-scalable=yes")
    meta(name="robots" content="noindex")

    if settings.env == 'development'
      link(rel='stylesheet', href='/css/main.css')
    else
      link(rel='stylesheet', href='/dist/css/main.min.css')
  body
    include includes/head.pug
    block content
    include includes/footer.pug
